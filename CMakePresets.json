{
    "version": 8,
    "configurePresets": [
        {
            "name": "cmake-build",
            "displayName": "Custom configure preset",
            "description": "Sets build and install directory",
            "binaryDir": "${sourceDir}/${presetName}",
            "cacheVariables": {
                "CMAKE_BUILD_TYPE": "Debug",
                "EASYCPPSOCKETS_BUILD_TESTS": "ON"
            }
        }
    ]
}